import { PipePercentPipe } from './pipe-percent.pipe';

describe('PipePercentPipe', () => {
  it('create an instance', () => {
    const pipe = new PipePercentPipe();
    expect(pipe).toBeTruthy();
  });
});
