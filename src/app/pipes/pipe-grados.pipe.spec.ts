import { PipeGradosPipe } from './pipe-grados.pipe';

describe('PipeGradosPipe', () => {
  it('create an instance', () => {
    const pipe = new PipeGradosPipe();
    expect(pipe).toBeTruthy();
  });
});
